name: sso
services:
  redis:
    extends:
      file: dc.base.yaml
      service: redis

  db:
    extends:
      file: dc.base.yaml
      service: db

  minio:
    extends:
      file: dc.base.yaml
      service: minio

  backend:
    extends:
      file: dc.base.yaml
      service: backend

volumes:
  pg_data:
  redis_data:
  minio_data:

networks:
  sso:
    name: sso